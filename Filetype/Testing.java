// Java class are normally (always) in so called packages, but in this
// exercise we skip that

public class Testing { 

    // This is really not a very useful class, but good enough to use
    // for checking file types.

}
